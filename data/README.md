The dataset used in this project is Google Speech Commands.

Due to repository size limitations the raw dataset is not stored in GitHub.

The dataset is loaded automatically using:

load_dataset("google/speech_commands")